import { LoaderContainer, StyledSpinner, Circle } from './Loader.styles'

const Spinner = () => (
  <LoaderContainer>
    <StyledSpinner>
      <Circle />
    </StyledSpinner>
  </LoaderContainer>
)

export default Spinner
